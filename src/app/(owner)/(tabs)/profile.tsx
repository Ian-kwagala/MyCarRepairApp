// Owner Profile tab: the shared profile screen with no owner-specific extras.
import { ProfileScreen } from '@/features/profile-screen';

/** Owner's Profile tab. */
export default function OwnerProfile() {
  return <ProfileScreen />;
}
